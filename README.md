# FashionWeb-ReactJS-NextJS

src/
│── assets/        # ảnh, icon
│── components/    # component dùng chung
│── pages/         # các trang (Home, Login, About…)
│── services/      # gọi API
│── hooks/         # custom hooks
│── utils/         # hàm dùng chung
│── App.jsx
│── main.jsx

